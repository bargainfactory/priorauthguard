apiVersion: 1

providers:
  - name: PriorAuthGuard
    orgId: 1
    folder: PriorAuthGuard
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
